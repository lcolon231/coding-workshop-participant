# Development and test dependencies. NOT shipped to Lambda -- the runtime
# dependency set lives in backend/<service>/requirements.txt and is == pinned.
# Lower bounds here rather than exact pins: these never reach production.
uvicorn[standard]>=0.34
pytest>=8.3
pytest-cov>=6.0
# Random ordering is the cheapest detector for shared-fixture bleed between tests.
pytest-randomly>=3.16
httpx>=0.28
freezegun>=1.5
ruff>=0.8
bandit>=1.8
