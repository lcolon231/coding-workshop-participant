# The email domain self-registration accepts. Must match the backend's
# `signup_domain` setting; the frontend only uses it for hints and a
# pre-submit check, the backend is the authority.
VITE_SIGNUP_DOMAIN=acme.inc
